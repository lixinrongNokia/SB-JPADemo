server:
  port: 8080
  servlet:
    context-path: /SbLayuiDemo
spring:
  devtools:
    restart:
      enabled: true
      exclude: /console
  datasource:
    username: lixinrong
    password: lixinrong
    url: jdbc:mysql://127.0.0.1:3306/administrative_block?Unicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 50
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true